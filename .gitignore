# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets — never commit your real keys
.env

# Editor / local history
.history/
.vscode/

# The built index (regenerate with `askrepo index`; it's output, not source)
index/

# Eval runs are outputs — except the frozen baseline, which IS committed
evals/runs/
